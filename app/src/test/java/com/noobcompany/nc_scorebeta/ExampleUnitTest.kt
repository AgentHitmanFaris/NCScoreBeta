package com.noobcompany.nc_scorebeta

import org.junit.Test

import org.junit.Assert.*

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * This class serves as a placeholder for unit tests that do not require the Android framework.
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class ExampleUnitTest {

    /**
     * A simple test to verify basic arithmetic assertions.
     *
     * This test confirms that the testing framework is correctly set up.
     */
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
